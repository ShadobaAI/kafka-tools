# Прежние примеры обмена

[← Инструменты AsyncAPI](../../README.md)

Здесь сохранены пары YAML/XSD: `asyncapi_example`, `kfk-test` и `kfk-unit`. Их содержимое не изменено при переносе.

Эти спецификации содержат формы за пределами поддерживаемого профиля генератора. Для запуска текущего генератора используйте [supported-example.yaml](../../generator/supported-example.yaml). Сохранённые XSD не являются результатом проверки новой версии генератора.
