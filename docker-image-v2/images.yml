defaults:
  registry: ghcr.io/<org>
  revision: r1
  platform: linux/amd64

versions:
  edt: []
  platform: []

profiles:
  edtcli:
    image: "{registry}/edtcli:latest"

  ibcmd:
    image: "{registry}/ibcmd:latest"

  client:
    image: "{registry}/client:latest"
