interface:
  display_name: "1C Standards"
  short_description: "Evidence-backed 1C standards and diagnostics"
  default_prompt: "Use $1c-standards to retrieve and apply the relevant 1C rule or diagnostic evidence."

dependencies:
  tools:
    - type: "mcp"
      value: "v8std"
      description: "User-configured standards, diagnostics, and snippet corpus"
      transport: "streamable_http"
