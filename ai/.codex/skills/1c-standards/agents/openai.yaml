interface:
  display_name: "1C Standards"
  short_description: "General 1C policy without corporate"
  default_prompt: "Use $1c-standards to retrieve and apply relevant general 1C evidence without the corporate collection."

dependencies:
  tools:
    - type: "mcp"
      value: "v8std"
      description: "General 1C, diagnostics, and snippet corpus"
      transport: "streamable_http"
