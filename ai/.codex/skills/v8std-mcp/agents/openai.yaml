interface:
  display_name: "v8std MCP"
  short_description: "Targeted 1C standards and diagnostic lookup"
  default_prompt: "Use $v8std-mcp to retrieve only the 1C rule or diagnostic evidence needed for this question."

dependencies:
  tools:
    - type: "mcp"
      value: "v8std"
      description: "1C standards, diagnostics, and patterns from v8std.ru"
      transport: "streamable_http"
      url: "https://ai.v8std.ru/mcp"
