interface:
  display_name: "v8std MCP"
  short_description: "Evidence-based 1C standards and diagnostics"
  default_prompt: "Use $v8std-mcp to apply relevant 1C standards, verify diagnostic findings, and cite the exact authoritative pages."

dependencies:
  tools:
    - type: "mcp"
      value: "v8std"
      description: "1C standards, diagnostics, and patterns from v8std.ru"
      transport: "streamable_http"
      url: "https://ai.v8std.ru/mcp"
